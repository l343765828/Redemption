# S6-01～S6-06 六轮审计意见真实性核验与反驳表

> 固定代码事实基线：`2475c6c49e60089b28f8ef1c0b75e86b2ceb6ebb`  
> 本表只裁决文档和控制程序缺陷；不把设计片段写成已实施代码，不把控制脚本自测写成项目 DEV/UAT 结果。

| 编号 | 核验结论 | 事实依据 | 第七轮修补动作 |
|---|---|---|---|
| S6-01 Traceability duplicate-shadow 漏检 | **正确／应采纳** | 旧 `metadata_value()` 命中首行即返回；`parse_modplan()` 对重复非核心项执行 `continue`，第二个冲突定义不会进入集合差。 | 元数据标签必须恰好出现一次；MOD 非核心项、REPORT REM/W/V 边重复即失败；新增 TASK/WORK 重复元数据、MOD 冲突状态和 REPORT 重复边负例。 |
| S6-02 前置 WORK 身份由 provenance 自证 | **正确／应采纳** | 旧校验器只证明 Git 历史等于输入文件自报的 `work_id→commit/tree`，任意提交可冒充前置 WORK。 | 新增规范 `WORK_APPROVED_COMMIT_REGISTRY.json`；每个 included WORK 必须与注册表中的 APPROVED commit/tree、patch/scope/provenance/approval 哈希及批准身份逐字段一致。注册表 SHA 同时绑定到 parent provenance 与 DEV 证据。 |
| S6-03 WORK→MODPLAN 归档哈希漂移 | **正确／应采纳** | WORK 总方案旧值为 `cee75c…`，随包 MODPLAN ZIP 实测为 `950dcf5629661fa40008560d0460e4cc00bf139ef632c8784c273672db8a950d`。 | WORK 总方案、版本引用清单、顶层 manifest 统一绑定实测 SHA；版本校验器直接重算内嵌 ZIP。 |
| S6-04 顶层文件计数字段错误 | **正确／应采纳** | 旧字段名表示“排除根 SHA 后文件数”，实际值却是根 SHA 条目数。 | 分拆为 `package_file_count_total`、`package_file_count_excluding_root_sha256`、`root_sha256_entry_count`，归档完成后按实物重算。 |
| S6-05 自测临时目录预检缺失 | **正确／应采纳** | 无 `/tmp` 且未设置 `TMPDIR` 时，旧自测在 `mktemp` 前退出。 | 新增 `ensure_temp_root.sh`；所有活动自测入口显式选择/创建可写临时根。Patch/DEV 门禁直接使用证据目录下临时子目录，不依赖系统 `/tmp`。 |
| S6-06 非有限 Decimal 缺显式领域异常 | **正确／应采纳** | `Decimal('NaN')/Infinity` 旧设计依赖后续 exponent 运算偶然抛 `TypeError`，错误合同不稳定。 | `parse_db_amount_to_units()` 在进入缩放前执行 `value.is_finite()`，NaN、sNaN、±Infinity 明确抛 `ValueError('DB amount must be finite')`，并补用例。 |

## 分域结论

- 文档与控制程序技术阻断 S6-01～S6-06：本轮定点闭合。
- 文档技术施工就绪度：`APPROVED_FOR_CONSTRUCTION_CANDIDATE`。
- 组织授权：`PENDING_ORGANIZATIONAL_APPROVAL`；当前材料没有可核验批准人、角色和签名，不能由 AI 代签。
- 代码审计：`REJECTED`；真实实现、patch、DEV、UAT 均未执行。
- DEC-013 / Gate C：`OPEN / OPEN`。
