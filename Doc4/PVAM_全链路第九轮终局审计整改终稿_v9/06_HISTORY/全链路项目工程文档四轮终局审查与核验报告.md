# 全链路项目工程文档四轮终局审查与核验报告

## 0. 审查口径与结论边界

本报告严格按以下证据顺位裁决：当前审查指令 ＞ `PLAN-PVAM-v1.15` 及其 §17 DEC 决议 ＞ `REPORT-PVAM-v1.5` ＞ 《F3-01～F3-10 真实性核验与反驳表》＞ 代码基线 `2475c6c49e60089b28f8ef1c0b75e86b2ceb6ebb` ＞ 有效 `sql_uat/` ＞ MODPLAN/TASK/WORK 转述。

核验动作限于压缩包与固定 Git 快照的只读解析、检索、AST/JSON/Shell 静态检查及沙箱内最小函数/合成仓库复现；未连接、调用或模拟 MySQL、Redis、Kafka、Dask、GPU、Kubernetes 或其他外部系统。凡涉及真实实施、部署、DEV 全套结果或 UAT 结果，均不作推定。

本报告使用两层结论：

- **文档治理准入**：判断材料是否具备进入组织审批并下发施工的内部一致性与可执行门禁；
- **代码运行验证**：继续受真实 patch、DEV、UAT、部署与 Gate C 约束，不因文档通过而自动通过。

---

## 1. 材料自检与基线复验结果

### 1.1 压缩包完整性与材料登记

| 材料 | 实际读取结果 | 自检结论 |
|---|---|---|
| 原始压缩包 `PVAM_全链路第四轮定点修订套件_v2.zip` | SHA-256：`76257fe37f708c236968c14d34b93a3cb6986b610046257875bb35319db4d67f`；71 个文件条目 | `CONFIRMED` |
| ZIP 结构 | `unzip -t` 返回 “No errors detected”；无绝对路径、`..` 越界路径或符号链接 | `CONFIRMED` |
| 根目录校验 | 根 `SHA256SUMS.txt` 全部通过；MODPLAN、WORKPLAN 子套件各自 `SHA256SUMS.txt` 全部通过；JSON 均可解析 | `CONFIRMED` |
| 生产方处置表 | `00_F3-01-F3-10_真实性核验与反驳表.md` | 已完整读取 |
| 检查方案 | `01_PLAN/Redemption_PV_Amount_Migration_d74_检查方案_v1.15.md`，编号 `PLAN-PVAM-v1.15` | 已完整读取 |
| 复核报告 | `02_REPORT/REPORT-PVAM-v1.5.md` 与长文件名副本 | 两文件 SHA-256 同为 `3df2fb9f3032bbec7bb78bbac13a6885d5f1f5b9d09d3d820b3e32b9332feb05`，内容相同，按一份受控报告计 |
| 修改方案套件 | `MODPLAN-PVAM_v1.2_总方案.md`、TASK-01～06、07A、07B、08，共 1 份总方案 + 9 份任务书 | 已完整读取 |
| 施工方案套件 | `WORK-PLAN-PVAM_v1.3_施工总方案.md`、WORK-01～06、07A、07B、08，共 1 份总方案 + 9 份任务书 | 已完整读取 |
| 治理控制文件 | `TRACEABILITY_MANIFEST.v2.json/.md`、`PATCH_DELIVERY_POLICY.md`、`DEV_VALIDATION_POLICY.md`、`STATUS_SCHEMA.md`、`ROLLBACK_MANIFEST_SPEC.md`、授权/基线/版本 manifest、5 个控制程序、9 个 WORK 测试命令及自测证据，共 33 个受控文件 | 已读取并按适用范围复验 |
| 历史授权记录 | `06_HISTORY/APPROVAL_RECORD-PVAM-20260805_UNVERIFIED.md` | 已读取；文件自身明确为 `UNVERIFIED / SUPERSEDED`，不能作为施工授权 |

控制脚本静态检查结果：所有 `05_CONTROL/*.sh` 与 `work-test-commands/*.sh` 均通过 `bash -n`；3 个 Python 校验器通过 `py_compile`；原始 Traceability 与版本引用正向自测分别输出 `TRACEABILITY_V2_PASS`、`VERSION_REFERENCE_PASS`。这些结果只证明相应正向样例被脚本接受，不证明脚本具备其宣称的完整反向校验能力。

### 1.2 固定代码基线复验

在只读快照 `Redemption-2475c6c` 上实测：

```text
git rev-parse HEAD
2475c6c49e60089b28f8ef1c0b75e86b2ceb6ebb

git cat-file -t 2475c6c49e60089b28f8ef1c0b75e86b2ceb6ebb
commit

git status --porcelain=v1
<空输出>

check_baseline_preflight.sh
BASELINE_PREFLIGHT_PASS AUDIT4 2475c6c49e60089b28f8ef1c0b75e86b2ceb6ebb
```

裁决：HEAD 精确命中要求的 40 位提交，索引与工作树干净，固定基线真实存在，结论为 `CONFIRMED`。`097cae32...2475c6c` 的 Git 文件级比较仅命中 `Doc/Elite_Bonus_发奖规则说明.docx`；PLAN/REPORT 已把 `097cae32` 降格为 `HISTORICAL_ONLY`，Python/SQL 锚点在当前提交重新取证，基线迁移事实成立。

### 1.3 当前受控状态

根 `DOCUMENT_MANIFEST.json` 的实际字段为：

```json
{
  "package_version": "round4-v2",
  "document_status": "DRAFT",
  "authorization_status": "PENDING_ORGANIZATIONAL_APPROVAL",
  "implementation_status": "BLOCKED",
  "validation_status": "PENDING_TEST_ENV",
  "baseline_commit": "2475c6c49e60089b28f8ef1c0b75e86b2ceb6ebb",
  "plan": "PLAN-PVAM-v1.15",
  "report": "REPORT-PVAM-v1.5",
  "modplan": "MODPLAN-PVAM_v1.2",
  "workplan": "WORK-PLAN-PVAM_v1.3",
  "code_audit_conclusion": "REJECTED",
  "dec_013": "OPEN",
  "gate_c": "OPEN",
  "real_work_patches_generated": false,
  "real_dev_executed": false,
  "real_uat_executed": false
}
```

上述状态本身诚实地区分了文档、授权、实施与验证，不能被解释为已经取得 `APPROVED_FOR_CONSTRUCTION`。

---

## 2. 【第四轮专节】F3-01～F3-10 四轮独立裁决矩阵

| 编号 | 生产方修补主张 | 独立核验与最小实测 | 裁决 | 证据与理由 |
|---|---|---|---|---|
| F3-01 | 补回 R-003、R-009 丢失 CHK 边，并保持 012A/012B 与 R-012 父项 | WORK 总方案 §9 的 R-003 行已含 `CHK-ARCH-003、CHK-BIZ-011`，R-009 行已含 `CHK-BIZ-006`；07A/07B 行使用 `R-012A/B`、`REM/W/V-012A/B` 且带 `parent=R-012`；Manifest 同样带 `parent_issue` | **`RESOLVED`** | 本项指定的三处定点修复真实落地。R-012 子项的完整 CHK 边在 Manifest 与子文档间仍不一致，归入 F3-02/八级追溯完整性，不反向否定本项“编号不再回退”的事实 |
| F3-02 | Traceability Manifest 升为 v2，补 `dec/step/tc/evidence/parent_issue`，并具备双向/孤儿校验 | JSON `schema_version=2`，14 行均具备所列字段，正向校验输出 `TRACEABILITY_V2_PASS rows=14 core=13`；但校验器只遍历 Manifest→单一 WORK 文件，未提取文档侧全部节点。向 WORK-01 副本新增孤儿 `TC-PVAM-01-999`、`EV-PVAM-01-999` 后，校验仍退出 0；Manifest 也没有 RISK/UV/OPT/GAP/FIX 行 | **`REJECT_REBUTTAL`** | v2 字段升级真实，但“双向/孤儿自动校验”主张不真实。`validate_traceability_v2.py` 第 27～32 行只验证 manifest 所列 STEP/TC/EV 在 WORK 中出现；不会发现文档新增的孤儿，也不读取 PLAN/REPORT/MOD/TASK。R-012A/B 的 Manifest CHK 集合还丢失子文档边，详见 §4 |
| F3-03 | PLAN/MOD/TASK/WORK 统一回退为 `DRAFT / PENDING_ORGANIZATIONAL_APPROVAL`，消除“已批准又未授权” | 总控字段及 §0 状态已回退；但 9 份 WORK 的第 3 行全部仍写“由**已经批准的** `TASK-PVAM-*` 转化”，同页 §0 又写 TASK/WORK 为 `DRAFT`、未授权、`BLOCKED` | **`REJECT_REBUTTAL`** | 结构化字段修复真实，语言矛盾仍在全体 WORK 子文档中系统性存在，不是单一笔误；历史批准文件虽移入 `UNVERIFIED`，但子文档仍把未批准 TASK 描述成已批准 |
| F3-04 | Patch 脚本改用仓库根目录、移除 `$OLDPWD` 依赖，在 `set -u` 下退出 0 | `validate_work_patch.sh` 实际使用 `REPO_ROOT=$(git -C "$REPO_ARG" rev-parse --show-toplevel)`，无 `OLDPWD` 引用；以 `env -u OLDPWD` 重跑 `selftest_patch_policy.sh`，得到 `PATCH_VALIDATION_PASS`、`PATCH_POLICY_SELFTEST_PASS`、`PATCH_POLICY_NEGATIVE_RENAME_PASS`，退出码 0 | **`RESOLVED`** | 原“未定义 OLDPWD 导致退出 1”已消除；本裁决仅确认脚本机制与该故障，不代表真实 WORK patch 已生成或全部 allowlist 正确 |
| F3-05 | Phase A 只检查基线既有目录；Phase B 只编译当前 WORK 的实际变更文件；08A/07A 不再依赖未来 `Common/` | `check_baseline_preflight.sh` 只检查 `Model/Order/MessageConsumer/User/Redishelper/sql_uat`；基线确无 `Common/Settlement/Ops`，脚本仍退出 0。`validate_work_dev.sh` 从 changed-files 选取 `.py` 并逐文件 `py_compile`；WORK-07A allowlist/测试命令不引用 `Common/` | **`RESOLVED`** | 原动态目录依赖环已解除。WORK 总方案 §9.1 仍保留泛化的 `check_baseline.sh; compileall` 描述，部分后置 WORK 步骤也保留目录级 compileall，属待清理的 `PLAN_GAP`，但未重新造成 Wave 0/07A 对 `Common/` 的隐式依赖 |
| F3-06 | DEV 证据绑定 `BASE + WORK_COMMIT + patch SHA + applied tree`，在干净 base worktree 应用 patch 后编译测试 | 字段与单 WORK 合成自测已实现；但脚本恒取 `diff BASE..WORK_COMMIT`。在合成依赖链中，WORK-02 commit 同时包含已完成的 WORK-01 `Common/PvAmount.py` 和自身修改，按 WORK-02 门禁运行即报 `out-of-scope paths: Common/PvAmount.py`。若 WORK-02 commit 不含 WORK-01，则干净 base tree 又缺前置公共层，无法验证依赖任务 | **`REJECT_REBUTTAL`** | 绑定字段存在，但未定义“当前 WORK 的父树/前置树”或可组合 patch 范围，机制与文档 Wave 2～6 的累积依赖不相容。另有两个真实范围错误：WORK-02 所需 `User/GlobalRecalculationService.py`、WORK-08 所需 `evidence/manifest.schema.json` 均被 allowlist 拒绝 |
| F3-07 | `validation_status=PENDING_TEST_ENV` 与 `artifact_status=PENDING` 解耦 | `STATUS_SCHEMA.md` 已正确拆域，EV 表也明确把 `PENDING` 标为工件状态；但 TASK-08 §11 第 5 项仍写“外部材料缺失时保持 `BLOCKED/PENDING`”，WORK-08 的“预期行为/目标”两处也保留同样无域限定的裸 `PENDING` | **`REJECT_REBUTTAL`** | 规范层修复成立，活动任务/施工文本未 100% 落地；该写法无法判断是验证状态还是工件状态，直接违反“裸 PENDING 只允许出现在明确 artifact_status 字段”的自身规则 |
| F3-08 | 活动版本统一为 PLAN 1.15 / REPORT 1.5 / MOD 1.2 / WORK 1.3，并由校验器约束 | 四份主文件及根 manifest 已对齐，正向校验通过；但 9 份 WORK 子文档的“文档版本”全部仍为 `v1.2`，同一文件版本记录又声明 `v1.2 SUPERSEDED`、`v1.3 DRAFT`。MOD 总方案仍把不存在于本包的 `00_F1-F7_审核意见核验与反驳表.md` 列作当前输入。版本校验器只扫描 4 个主文件，因此未发现上述问题 | **`REJECT_REBUTTAL`** | 主控版本已升级，子文档活动身份和当前引用未全套受控对齐；“校验器通过”不能证明全套版本纪律通过 |
| F3-09 | 修正 `trunc_div_zero` 的负分母向零截断 | 从 WORK-01 的 `DESIGN_FRAGMENT` 精确提取函数并执行：`3/2=1`、`3/-2=-1`、`-3/2=-1`、`-3/-2=1`、`1/-2=0`、`-1/2=0`、`0/-2=0`；分母 0 抛 `ZeroDivisionError`；全部断言通过 | **`RESOLVED`** | `abs` 商加符号异或算法覆盖四象限，已修复旧实现 `3/-2=-2` 的错误。该结论只证明设计片段算法正确；基线中 `Common/PvAmount.py` 仍不存在，不能写成代码已实现 |
| F3-10 | WORK-07B 默认禁止回滚恢复固定 MAXLEN；仅签署容量/告警/停止阈值后允许紧急例外 | TASK-07B 补充和 `ROLLBACK_MANIFEST_SPEC.md` 均正确；但 WORK-07B §11.3 仍写“无 `GHOST_IN_DOUBT` 且 `XLEN` 门禁通过才可恢复固定 MAXLEN 代码”，随后 §11.3A 才要求运维/架构双签和六类参数 | **`REJECT_REBUTTAL`** | 同一施工任务存在两个不同准入条件。§11.3 的较弱路径可在未签容量、速率、告警、停止阈值时恢复已知不安全代码，直接削弱 §11.3A；红线未闭合 |

### 2.1 F3 裁决统计

| 裁决 | 数量 | 编号 |
|---|---:|---|
| `RESOLVED` | 4 | F3-01、F3-04、F3-05、F3-09 |
| `ACCEPT_REBUTTAL` | 0 | — |
| `REJECT_REBUTTAL` | 6 | F3-02、F3-03、F3-06、F3-07、F3-08、F3-10 |

生产方“F3-01～F3-10 文档内可闭合问题已全部定点修订”的总体陈述不成立。局部修改多数可见，但“100% 采纳并闭环”被反向孤儿测试、治理/版本自相矛盾、依赖型 commit 门禁失败与 MAXLEN 双重标准直接否定。

---

## 3. 审核总结论

> # 【REJECTED】

本套件**未达到文档治理 `APPROVED / APPROVED_FOR_CONSTRUCTION` 的正式开工准入标准**，且不符合“仅余个别微观文字瑕疵”的 `APPROVED WITH CONDITIONS` 条件。原因是仍存在会改变施工范围、阻断依赖 Wave、漏检追溯孤儿或放宽数据安全红线的结构性错误，而非排版问题。

### 3.1 六大原阻断项终局状态

| 原阻断项 | 终局状态 | 评估 |
|---|---|---|
| 追溯断边与 012 编号 | 指定 R-003/R-009 边及 012A/B 编号已修复 | **已消除指定缺陷**，但全局八级追溯仍被 Manifest 缺口阻断 |
| Manifest 字段/自动校验 | v2 字段齐备，双向/孤儿能力不成立 | **未消除** |
| 治理状态冲突 | 总控字段回退，9 份 WORK 仍称 TASK 已批准 | **未消除** |
| Patch 脚本退出 1 | `OLDPWD` 故障已修，合成自测退出 0 | **已消除指定故障**；真实范围表另有阻断 |
| compileall 动态目录依赖环 | Phase A/Phase B 解耦，08A/07A 不依赖 `Common/` | **已消除指定故障** |
| DEV/COMMIT 门禁脱节 | 四元证据字段已加，但依赖 Wave 的 commit 范围不可执行 | **未消除** |

即按最窄口径计算，六项中只有 3 项完成关闭，另外 3 项仍未完成；同时新增确认了版本引用、状态枚举、allowlist 与 MAXLEN 回滚冲突。因此不具备由上一轮 `REJECTED` 升格为 `APPROVED` 的事实基础。

### 3.2 治理、代码与 UAT 分层结论

| 层级 | 当前结论 | 说明 |
|---|---|---|
| 文档技术闭环 | `REJECTED` | 追溯、治理、版本、门禁及回滚存在阻断性矛盾 |
| 组织施工授权 | `PENDING_ORGANIZATIONAL_APPROVAL` | `AUTHORIZATION_STATUS-PVAM-v2.md` 明确缺批准人、角色、签名、范围和不可抵赖记录；历史授权不可用 |
| 代码实施 | `BLOCKED / NOT_STARTED` | 根 manifest 明确真实 WORK patch 未生成；报告代码结论仍为 `REJECTED` |
| DEV 验证 | `GATED` | 单 WORK 合成机制部分成立；真实实施 commit、依赖树、范围表及完整自测未闭合 |
| UAT | `PENDING_TEST_ENV` | DEC-013 仍 OPEN，无真实外部环境证据 |
| 生产发布 | Gate C `OPEN` | DEC-009/010/013 所需材料、实现、恢复和 UAT 证据未完成；不得宣称生产就绪 |

REPORT 的 `FINAL` 只表示复核报告版本定版，不等于 PLAN/MOD/TASK/WORK 已批准，也不等于 13 项代码缺陷已关闭。

---

## 4. 全链路八级卡位对账与追溯矩阵

### 4.1 问题域双向对账

人工核对 MOD 总方案 §5 后，问题处置统计本身成立：

| 问题域 | 数量 | 文档状态 | 所属处置 | 人工对账 |
|---|---:|---|---|---|
| R-001～R-013 | 13 个核心缺陷 | `ACCEPTED` | TASK/WORK-01～07B | `CONFIRMED`，无增项或漏项；R-012A/B 只作父缺陷的两个施工子项，不把核心数增至 14 |
| RISK-001～002 | 2 | `UAT_VERIFY` | WORK-08，RISK-001 条件触发 WORK-06 | `CONFIRMED` |
| UV-001～005 | 5 | `UAT_VERIFY` | WORK-08 | `CONFIRMED` |
| OPT-001～002 | 2 | `ACCEPTED` 资产 | WORK-08 | `CONFIRMED` |
| GAP-DEC004-2B | 1 | `DEFERRED` | WORK-08 仅登记/fixture 边界 | `CONFIRMED` |
| FIX-001 | 1 | `CONFIRMED_CLOSED` | 文档基线 | `CONFIRMED`，不计入 13+7 |

因此，“13 个 ACCEPTED + 7 个 UAT_VERIFY + 2 个 OPT + 1 个 DEFERRED + 1 个 CLOSED”的**人工问题台账**无死角；但机器 Manifest 只包含 13 个核心缺陷的 14 个施工行，没有 RISK/UV/OPT/GAP/FIX 节点，不能声称机器双向对账达到 100%。

### 4.2 核心缺陷八级矩阵

下表以 WORK 总方案与 Manifest v2 为主，并回查 REPORT、TASK 与各 WORK 子文档。`STEP/TC/EV` 为 Manifest 中本地节点数量。

| CHK | R / parent | DEC | TASK → WORK | STEP / TC / EV | 独立裁决 |
|---|---|---|---|---:|---|
| DATA-001、DATA-003、EVT-002 | R-001 | 002、008、014 | 01 → 01 | 5 / 6 / 15 | `CONFIRMED`（正向链存在） |
| ARCH-003 | R-002 | 002、008、014 | 01 → 01 | 5 / 6 / 15 | `CONFIRMED`（正向链存在） |
| DATA-001、DATA-002、ARCH-003、BIZ-011 | R-003 | 002、005、006、007、010 | 02 → 02 | 7 / 8 / 17 | `CONFIRMED`；F3 丢边已补 |
| DATA-004、BIZ-007、BIZ-008 | R-004 | 001、002、003、009、014 | 03 → 03 | 6 / 7 / 15 | `CONFIRMED`（正向链存在） |
| DATA-006、BIZ-007 | R-005 | 004、016、018 | 04 → 04 | 5 / 8 / 16 | `CONFIRMED`（正向链存在） |
| DATA-006、BIZ-007、BIZ-008、BIZ-009、BIZ-011 | R-006 | 004、016、018 | 04 → 04 | 5 / 8 / 16 | `CONFIRMED`（正向链存在） |
| DATA-005 | R-007 | 002、005、006、007、010 | 02 → 02 | 7 / 8 / 17 | `CONFIRMED`（正向链存在） |
| BIZ-006、EVT-005 | R-008 | 007、008、011、017 | 05 → 05 | 6 / 8 / 16 | `CONFIRMED`（正向链存在） |
| BIZ-006、EVT-003、PUB-001 | R-009 | 007、008、010、012 | 06 → 06 | 6 / 9 / 18 | `CONFIRMED`；F3 丢边已补 |
| ARCH-002、EVT-003 | R-010 | 007、008、010、012 | 06 → 06 | 6 / 9 / 18 | `CONFIRMED`（正向链存在） |
| BIZ-005、BIZ-006、PUB-001 | R-011 | 007、008、011、017 | 05 → 05 | 6 / 8 / 16 | `CONFIRMED`（正向链存在） |
| Manifest：ARCH-002、EVT-006；07A 子文档另含 EVT-007、TEST-001、TEST-003 | R-012A，parent=R-012 | 010 | 07A → 07A | 4 / 7 / 13 | `PLAN_GAP`：子编号/父映射正确，但 Manifest 丢 3 条子文档 CHK 边 |
| Manifest：ARCH-002、EVT-006；07B 子文档另含 EVT-007、TEST-003 | R-012B，parent=R-012 | 007、010 | 07B → 07B | 6 / 9 / 15 | `PLAN_GAP`：REPORT 还少列 EVT-007；Manifest 丢 2 条子文档 CHK 边 |
| EVT-007 | R-013 | 007、010 | 07B → 07B | 6 / 9 / 15 | `CONFIRMED`（正向链存在） |

注：表中缩写 `DATA-001` 等均指 `CHK-DATA-001` 等完整编号。

### 4.3 Manifest v2 能力核验

| 检查点 | 原始正向样例 | 反向/负向测试 | 裁决 |
|---|---|---|---|
| schema 与必填字段 | 14 行均含 `issue/parent_issue/checks/decs/dec/task/work/rem/implementation/verification/steps/step/tests/tc/evidences/evidence` | — | `CONFIRMED` |
| R-003/R-009 强制边 | 校验器硬编码断言并通过 | — | `CONFIRMED` |
| 012 父子关系 | A/B 的 `parent_issue=R-012`，REM 为 012A/B | — | `CONFIRMED` |
| Manifest→WORK 的 STEP/TC/EV 存在性 | 正向样例通过 | 删除 Manifest 所列节点会失败 | `CONFIRMED`，仅单向 |
| WORK→Manifest 孤儿检测 | 未实现 | 在 WORK 副本加入 `TC-PVAM-01-999`、`EV-PVAM-01-999` 后仍输出 `TRACEABILITY_V2_PASS`、退出 0 | `REJECT_REBUTTAL` |
| PLAN/REPORT/MOD/TASK 跨层读取 | 校验器没有这些输入参数 | 无法验证这些层的文档独有节点与边 | `PLAN_GAP` |
| 非核心问题域 | JSON 无 RISK/UV/OPT/GAP/FIX 行 | 人工台账虽完整，机器链缺失 | `PLAN_GAP` |
| 受控 TC 与本地 TC | Manifest 只有 `TC-PVAM-*` 本地用例 | TASK AC 中的 `TC-001～032` 未单列为受控测试边 | `PLAN_GAP` |

结论：当前 Manifest 是“核心缺陷到 WORK 本地节点的单向索引”，不是生产方所称的全套八级双向事实源。

### 4.4 十项专项红线核验

| 红线 | 文档设计事实 | 裁决 |
|---|---|---|
| 1. DEC-018 不物化 Active | WORK-04 明确否决共享 Active table/snapshot；`ActiveRule` 纯计算且禁止持久化 | `CONFIRMED` |
| 2. DEC-004 fail-loud | provider 设计为 Redis→等待2秒→Redis→Delta→`RuntimeError`，禁止内置默认/回退 | `CONFIRMED` |
| 3. DEC-004 2B 不越权 | AR_CONFIG→Delta→Redis 写入/失效 producer 保持 `GAP-DEC004-2B / DEFERRED`；fixture 不得关闭生产链 | `CONFIRMED` |
| 4. DEC-012 Topology 归属 | WORK-06 承接 `TOPO-WIRE-01`；WORK-08 只提供 archive/deploy/callgraph 证据以选择验证或接线分支 | `CONFIRMED` |
| 5. DEC-007 引擎内部 Epoch | WORK-06 保留 Settlement Epoch、freeze/drain、run 状态、单调性、coverage 与恢复在本仓 | `CONFIRMED` |
| 6. DEC-008 Redis 权威 | WORK-05/06 把业务状态、revision、dirty/stage、outbox 放在 Redis 权威提交；业务系统 MariaDB 落库另责 | `CONFIRMED` |
| 7. DEC-009 schema 缺失即阻断 | WORK-08/总方案把 DBA 最小 schema manifest 缺失标为 `BLOCKED_EXTERNAL_EVIDENCE`，未推定通过 | `CONFIRMED` |
| 8. DEC-010 测试豁免不关闭生产 Gate | 文档保持 Raw/Normalized 生产材料测试阶段豁免，但 Gate C `OPEN` | `CONFIRMED` |
| 9. DEC-013 环境门禁 | 根 manifest、PLAN、MOD、WORK 均保持 DEC-013 `OPEN`，真实 UAT 未执行 | `CONFIRMED` |
| 10. Stream ACK/retention 回滚安全 | 正向设计要求 ACK-aware MINID、PEL/ghost/replay；但 WORK-07B §11.3 留有未签紧急例外即可恢复 fixed MAXLEN 的弱路径 | `REJECT_REBUTTAL` |

### 4.5 依赖拓扑与回滚安全

将 MOD 总方案 Mermaid 中 10 个节点、23 条有向边机器化拓扑排序，得到：

```text
DAG_PASS nodes=10 edges=23
order=08A,01,07A,02,03,04,05,06,07B,08B
```

因此文档声明的 Wave 图本身无环，WORK-07A 可与 WORK-01 并行，结论为 `CONFIRMED`。但“图无环”不等于“执行器可沿图运行”：F3-06 的固定 BASE patch 机制不能表示依赖任务的累积树，是门禁层面的逻辑死锁。

`ROLLBACK_MANIFEST_SPEC.md` 对 deployment、image、精确 argv、健康检查、状态快照 URI/SHA、数据恢复/重放、执行/复核/批准与演练证据的要求符合工程安全原则；各 WORK 在缺外部部署证据时标注 `BLOCKED_EXTERNAL_EVIDENCE` 也属合理。独立回滚整体仍不能通过，原因是：

1. 当前 9 个 WORK 均没有真实签署的 `ROLLBACK-MANIFEST-PVAM-v1.json`；
2. WORK-07B 的 fixed MAXLEN 弱回滚路径与统一规范冲突；
3. 真实部署对象、停止/恢复命令和隔离演练未提供，当前只能评价设计，不能评价可执行结果。

### 4.6 AC 与 DEV/UAT 分层

机器解析九组 TASK/WORK AC：

| WORK | AC 数量 |
|---|---:|
| 01 / 02 / 03 / 04 / 05 / 06 / 07A / 07B / 08 | 10 / 12 / 10 / 11 / 11 / 13 / 9 / 10 / 14 |
| 合计 | **100** |

结果：100 个 AC 的编号集合、环境列和受控 TC 列在 TASK→WORK 间全部一致；WORK 环境统计为 `DEV=14`、`UAT=19`、`DEV+UAT=67`。唯一非逐字项是 07A AC-01：TASK 写“空 payload 不返回成功，不在无处置证据时 ACK”，WORK 收窄为“返回 `UNHANDLED_KEEP_PEL`，不写 DLQ 且不 ACK”；后者是可兼容的具体化，不构成语义冲突。

DEV/UAT 分层设计总体清楚，真实中间件/数据库/GPU 项未冒充 DEV PASS；但 DEV 门禁因累计 commit 模型和 allowlist 错误仍不可稳定执行，因此本维度总体为 `PLAN_GAP`，不能据 100 条 AC 表格对齐宣称已具备施工准入。

---

## 5. 代码锚点、`trunc_div_zero` 函数复现与 Patch 脚本门禁对账表

### 5.1 报告更正函数名与基线锚点

| 报告锚点 | 基线实读片段 | 裁决 |
|---|---|---|
| `User/SuperEliteBonusService.py::calculate_se_bonus` | 第 209 行：`def calculate_se_bonus(` | `CONFIRMED` |
| `User/PEBonusService.py::execute_batch` | 第 341 行：`def execute_batch(self,` | `CONFIRMED` |
| `User/GlobalEliteBonusRecalculationService.py::settle_period` | 第 102 行：`def settle_period(self, period: str) -> None:` | `CONFIRMED` |

实际源码片段：

```python
# User/SuperEliteBonusService.py
def calculate_se_bonus(
    self,
    period: Any,
    calc_month: Any,
    ...
) -> dd.DataFrame:

# User/PEBonusService.py
def execute_batch(self,
                  period_num: int,
                  calc_month: int,
                  ...) -> Dict[str, dask_cudf.DataFrame]:

# User/GlobalEliteBonusRecalculationService.py
def settle_period(self, period: str) -> None:
    """单期全量 Elite Bonus 结算重算主入口"""
```

对 WORK 中重点锚点执行 AST/路径复验：42 个声明为基线既有的关键类/函数全部存在；82 个 CHG 路径/类型声明中，“新增”路径在基线不存在、“修改”路径在基线存在的分类未发现反置。`Common/PvAmount.py`、`Common/`、`Settlement/`、`Ops/` 在固定基线均不存在，WORK 将相关对象标为新增是事实。

基线还实读到以下未关闭代码事实，与 REPORT v1.5 一致：

```python
# MessageConsumer/RecalcStreamConsumer.py
if not payload_str:
    return True

# 多个 producer
pipe.xadd(..., maxlen=100000, approximate=True)
```

固定 `maxlen=100000` 共命中 6 处：GlobalRecalculationService 2 处、PlacementIncrementalService 1 处、PlacementRecalculationService 2 处、GlobalEliteBonusRecalculationService 1 处。故 WORK-07A/07B 的问题锚点真实，但仍是“待施工”，不是已修代码。

### 5.2 `Common/PvAmount.py` 设计与 `trunc_div_zero` 复现

WORK-01 明确把代码标为 `DESIGN_FRAGMENT`，这一边界是正确的。其最低合同包括：拒绝 bool/非整数、int64 边界检查、外部 Decimal 字符串解析、micro-unit 精度检查、金额版本严格验证及向零除法。静态解析未发现语法错误。

被测函数原文：

```python
def trunc_div_zero(numerator: int, denominator: int) -> int:
    """整数除法向零截断；支持任意非零分母。"""
    if denominator == 0:
        raise ZeroDivisionError("denominator must not be zero")
    quotient = abs(numerator) // abs(denominator)
    return -quotient if (numerator < 0) ^ (denominator < 0) else quotient
```

精确提取并执行该函数的结果：

```text
3/2=1
3/-2=-1
-3/2=-1
-3/-2=1
1/-2=0
-1/2=0
0/-2=0
1/0=ZeroDivisionError
TRUNC_DIV_ZERO_EXACT_FRAGMENT_PASS
```

裁决：算法为 `CONFIRMED`；但 `Common/PvAmount.py` 在 `2475c6c4` 树中不存在，且 `real_work_patches_generated=false`，所以实现状态仍为 `NOT_STARTED`，运行验证仍 `GATED`。

### 5.3 Patch、版本与 DEV 控制实测表

| 控制项 | 实测 | 结果 |
|---|---|---|
| Shell/Python/JSON 基础有效性 | Shell 全部 `bash -n`；Python 校验器 `py_compile`；JSON `jq` 解析 | `CONFIRMED` |
| 基线 preflight | 固定 SHA、6 个基线既有目录均通过；不要求未来目录 | `CONFIRMED` |
| Patch `$OLDPWD` 故障 | `env -u OLDPWD` 下完整合成自测退出 0 | `CONFIRMED` |
| Patch scope rename 负例 | 自测输出 `PATCH_POLICY_NEGATIVE_RENAME_PASS` | `CONFIRMED` |
| Traceability 正向样例 | 原包输出 `TRACEABILITY_V2_PASS` | `CONFIRMED`，仅正向 |
| Traceability 反向孤儿 | 文档副本新增未入 manifest 的 TC/EV，校验仍退出 0 | `REJECT_REBUTTAL` |
| 版本正向样例 | 校验器输出 `VERSION_REFERENCE_PASS` | `CONFIRMED`，只扫描 4 个主文件 |
| 版本全套能力 | 9 个 WORK 元数据均为旧 `v1.2`，校验仍通过 | `REJECT_REBUTTAL` |
| DEV 四元绑定 | JSON 设计包含 base/work commit/patch SHA/applied tree | `CONFIRMED`（单 WORK 机制） |
| DEV 依赖 Wave | 累计 WORK-02 commit 被 WORK-02 scope 以 `Common/PvAmount.py` 越界拒绝，退出 1 | `REJECT_REBUTTAL` |
| WORK-02 allowlist | 输入 `M User/GlobalRecalculationService.py`；校验输出 `out-of-scope paths`，退出 1 | `PLAN_GAP` |
| WORK-08 allowlist | 输入 `A evidence/manifest.schema.json`；校验输出 `out-of-scope paths`，退出 1 | `PLAN_GAP` |
| DEV 自测在本沙箱的完整重跑 | Patch 阶段通过；`validate_work_dev.sh` 第 38 行因本沙箱无 `/dev/fd/63` 而退出 1 | `UNVERIFIABLE`：属执行环境限制，不单独判脚本业务失败；上述范围/依赖失败不依赖此限制 |
| 真实 WORK patch / DEV / UAT | 根 manifest 均为 `false` | `NOT_RUN / PENDING_TEST_ENV` |

依赖型 commit 的最小反例说明：脚本固定生成 `git diff BASE WORK_COMMIT`，再以“当前 WORK” allowlist 检查全部差异。后继 commit 若包含前置任务，则被判越界；若不包含前置任务，则干净 BASE worktree 缺依赖。当前文档没有第三种可执行模型，因此 F3-06 是结构性阻断。

---

## 6. 发现的具体纰漏与修改建议清单

| 编号 | 位置 / 文档 | 问题类型 | 表现与对账证据 | 推荐修正方案 | 阻断级别 |
|---|---|---|---|---|---|
| B4-01 | `validate_traceability_v2.py` / Manifest v2 | 追溯漏检 | 只做 Manifest→WORK 字符串存在性；注入文档孤儿 TC/EV 后仍 PASS；不读 PLAN/REPORT/MOD/TASK | 从各受控文档解析节点/边并与 manifest 做集合双向差；对缺失边、文档孤儿、manifest 孤儿、重复 ID、非法父子关系分别非 0 退出 | **阻断** |
| B4-02 | Manifest R-012A/B | 追溯边丢失 | A 只列 ARCH-002/EVT-006，子文档另有 EVT-007/TEST-001/TEST-003；B 子文档另有 EVT-007/TEST-003 | 按 PLAN/REPORT 权威顺位统一 R-012A/B 的完整 CHK 集；为 R-012 父项只保留聚合关系，不创建会被误当施工节点的泛化 REM/W/V | **阻断** |
| B4-03 | Manifest v2 / WORK-08 | 范围不完整 | 无 RISK/UV/OPT/GAP/FIX 行，也无受控 `TC-001～032` 边；与“全套双向追踪”不符 | 扩展 schema 或另建强制分区，完整覆盖非核心问题域及 controlled TC↔local TC 映射；计数规则明确父子不重复 | **阻断** |
| B4-04 | 9 份 `WORK-PVAM-*` 首段 | 治理状态冲突 | 全部写“由已经批准的 TASK 转化”，同页又是 DRAFT/PENDING/BLOCKED | 将“已经批准”改为“待组织批准的/来源于”；取得真实授权后再由同一受控变更统一升格，不能仅改一处状态 | **阻断** |
| B4-05 | 9 份 WORK 文档控制 | 活动版本错配 | 元数据写 v1.2，版本表写 v1.2 SUPERSEDED、v1.3 DRAFT | 统一子文档身份为 v1.3，并把全部 9 份纳入版本校验器 `active_files` 或按目录模式强制扫描 | **阻断** |
| B4-06 | MODPLAN v1.2 §1、§12 | 陈旧活动引用 | 两处把包内不存在的 `00_F1-F7_审核意见核验与反驳表.md` 列为当前输入 | 当前链改为 F3-01～10；若需保留历史文件名，移到明确的 `HISTORICAL_ONLY` 表并禁止参与活动校验 | 重要 |
| B4-07 | `validate_work_patch.sh` / `validate_work_dev.sh` | 依赖 commit 模型不可执行 | 固定 BASE→WORK_COMMIT 与 per-WORK scope 无法同时表示前置任务后的累积树 | 明确并实现每 WORK 的 `PARENT_TREE_SHA`/前置集 tree；patch 只取 parent→work commit，另绑定 prerequisite tree hash；或使用每 WORK 独立补丁并在组合树上二次验证，二者择一且增加多 Wave 自测 | **阻断** |
| B4-08 | `WORK_SCOPE_ALLOWLIST.json` WORK-02 | 范围表漏项 | WORK-02 要改并编译 `User/GlobalRecalculationService.py::_get_previous_period`，allowlist/CHG 表却无该文件；实测退出 1 | 先由 TASK/WORK CHG 表确认是否属于 WORK-02，再同步 allowlist、STEP、测试与回滚范围；禁止仅放宽 allowlist 而不补追溯 | **阻断** |
| B4-09 | `WORK_SCOPE_ALLOWLIST.json` WORK-08 | 范围表路径错误 | CHG-01 为 `evidence/manifest.schema.json`，allowlist 允许的是 `evidence_schema/`；实测退出 1 | 在设计中选择唯一受控路径并同步 CHG、STEP、allowlist、证据 URI 与测试；不得同时保留两个目录语义 | **阻断** |
| B4-10 | TASK-08、WORK-08 | 状态枚举未落地 | 活动文本仍写无域限定 `BLOCKED/PENDING` | 验证状态明确写 `BLOCKED/PENDING_TEST_ENV`；若指工件，明确写 `artifact_status=PENDING` | 重要 |
| B4-11 | WORK-07B §11.3 与 §11.3A | 回滚红线冲突 | §11.3 只凭 ghost/XLEN 即允许 fixed MAXLEN，绕过后文双签和六类容量/告警条件 | 删除较弱许可路径；所有恢复 fixed MAXLEN 的文字只引用统一紧急例外门禁，并加入静态规则禁止出现未限定的“可恢复固定 MAXLEN” | **阻断** |
| B4-12 | WORK 总方案 §9.1、部分 WORK STEP | DEV 描述漂移 | 总表仍写 `check_baseline.sh; compileall`，发布控制实际为 `check_baseline_preflight.sh` + changed-file `py_compile`；两套口径易导致执行者绕开 v2 控制 | 指定 `05_CONTROL` 脚本为唯一执行入口；目录级 compileall 如保留，应标为附加检查且不得取代 per-WORK changed-file 门禁 | 重要 |
| B4-13 | 07A AC-01 | AC 文本非逐字 | TASK 与 WORK 标准语义相容但文案不同 | 若要求生成式 100% 对账，统一为同一规范文本；可在 WORK 增加“具体 disposition”列，不改来源 AC 原文 | 轻微 |
| B4-14 | `AUTHORIZATION_STATUS-PVAM-v2.md` | 外部准入未满足 | 批准人、角色、签名、范围、允许 Wave 均缺；历史记录明确不可用 | 在全部文档阻断项关闭后，取得可核验组织授权；批准记录必须列明仍为 BLOCKED/DEFERRED/PENDING_TEST_ENV 的事项 | **外部门禁** |

上述 B4-01、04、05、07、08、09、11 任一单独存在即足以阻止 `APPROVED_FOR_CONSTRUCTION`；它们不能按“微观文字瑕疵”处理。

---

## 7. 后续代码施工与 UAT 准入步骤建议

当前顺序必须先关闭文档与控制面阻断，再申请施工授权；不得在 `REJECTED` 状态下直接进入 Wave 0。

1. **关闭追溯控制缺口**：统一 R-012A/B 边，覆盖 RISK/UV/OPT/GAP/FIX 与 controlled/local TC；新增真正双向的节点/边集合校验及孤儿、重复、父子负例。
2. **统一治理和版本身份**：修正 9 份 WORK 的“已经批准”与 v1.2 元数据，清理 MOD 活动旧引用；把全部主/子文档加入版本校验。
3. **重构 WORK commit 模型**：为每个 WORK 固定前置 tree/parent SHA、直接变更范围和组合后 tree；增加至少 `01→02→05→06→07B` 的多 Wave 合成回归，不得只测无依赖的 07A。
4. **修正范围清单**：逐项从 CHG/STEP 自动生成或交叉验证 allowlist；至少关闭 WORK-02 `GlobalRecalculationService.py` 与 WORK-08 `evidence/manifest.schema.json` 两个可复现拒绝。
5. **统一回滚红线**：删除 WORK-07B 较弱 fixed MAXLEN 路径；所有 WORK 的真实 rollback manifest 在隔离环境演练并签署后才能获得部署资格。
6. **重新执行文档门禁**：要求原始 Manifest 正向、文档孤儿负向、manifest 孤儿负向、版本子文档负向、rename/scope 负向、多 Wave patch/DEV 自测全部按预期退出；保存命令、exit、stdout/stderr 与 SHA-256。
7. **取得组织授权**：在上述控制面全部通过后，补齐批准人实名/角色、权限依据、签名原文、时间、基线、允许 TASK/WORK/Wave 和保留阻断项；再将文档状态受控升格，不能用当前审查指令代签。
8. **按依赖图实施**：08A →（01 与 07A）→（02 与 03）→04→05→06→07B；每个 WORK 先生成真实 commit/patch，再在绑定的前置 tree 上通过 patch、scope、tree hash、changed-file 编译、专属测试和零改动门禁。
9. **DEV 准入**：每个证据包至少绑定 base/parent、WORK commit、patch SHA、applied tree、测试命令、退出码、JUnit、后测工作树/index/untracked 状态；任何依赖外部系统的断言保持 `PENDING_TEST_ENV`。
10. **UAT 准入**：DEC-013 提供环境、权限、镜像、数据、DBA 最小 schema manifest 后，执行 100 条 AC 对应的 UAT 子集、SQL-Python 同数据差分、Redis 权威提交、ACK/PEL/trim/ghost、崩溃恢复和 rollback 演练；不得以 fixture 关闭 GAP-DEC004-2B。
11. **生产 Gate C**：只有 DEC-009/010/013 材料、真实 UAT、部署/恢复证据及全部 P0/P1 关闭后，才可另行评审 Gate C；在此之前持续保持 `OPEN`，文档批准也不得改写为生产发布批准。

终局准入判定：**文档治理未通过；施工不得下发；代码运行验证继续 `GATED BY DEV/UAT`；生产 Gate C 保持 `OPEN`。**
