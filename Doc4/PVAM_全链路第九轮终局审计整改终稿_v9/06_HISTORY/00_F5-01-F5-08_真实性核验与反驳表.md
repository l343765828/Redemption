# F5-01～F5-08 五轮审计意见真实性核验与反驳表

| 意见编号 | 标题 | 核验结论 | 事实依据 | 第六轮修补动作 |
|---|---|---|---|---|
| F5-01 | Traceability 校验器五层双向孤儿检测 | 【正确/应采纳】 | 第五轮校验器只对 WORK 的 STEP/local-TC/EV 做精确集合差；REPORT 注入 `R-999`、TASK 单侧删除 `CHK-TEST-001` 仍可能被上游字符串并集掩盖。 | 发布唯一入口 `validate_traceability_v3.py`；分别解析 PLAN/REPORT/MODPLAN/TASK/WORK 的受控节点和有向边，与 Manifest 的逐层 inventory/contract 做 missing/extra/duplicate 双向集合差。新增 PLAN、REPORT、TASK、WORK、Manifest、parent 和 duplicate 负例。 |
| F5-02 | 补齐 TC-020 与非核心域状态硬绑定 | 【正确/应采纳】 | 原 `controlled_test_mappings` 并集只有 31 项，缺少 PLAN/TASK 中真实存在的 `TC-020`；非核心状态只校验通用合法集合，无法阻断 RISK 被改为 ACCEPTED。 | 受控 TC 并集精确固定为 `TC-001～TC-032`；`TC-020` 明确进入 WORK-08 映射。RISK/UV=`UAT_VERIFY`、OPT=`ACCEPTED`、GAP=`DEFERRED`、FIX=`CONFIRMED_CLOSED` 按域硬校验。 |
| F5-03 | 控制文件名漂移与 WORK 总表补边 | 【正确/应采纳】 | 活动控制目录同时存在 `.v2` 别名和 v2 脚本名，但 JSON 已是 schema 3；WORK 总方案曾遗漏 R-012A/B 的 EVT/TEST 边。 | 活动控制目录仅保留 `TRACEABILITY_MANIFEST.json/.md` 与 `validate_traceability_v3.py`；旧别名移入 `06_HISTORY`。WORK 总方案补齐 R-012A/B 全部 CHK 边。 |
| F5-04 | WORK-08 状态字段解耦 | 【正确/应采纳】 | WORK-08 设计测试仍构造 `{"status":"BLOCKED"}`，与 `STATUS_SCHEMA` 的 `validation_status/artifact_status` 分域规则冲突。 | 改为 `validation_status="BLOCKED"`；状态规范明确禁止证据合同使用无域 `status` 表达 PASS/FAIL/BLOCKED/PENDING。 |
| F5-05 | 清理旧 DEV 自测入口 | 【正确/应采纳】 | 旧 `selftest_dev_policy.sh` 使用已废弃 CLI，缺 `--parent-commit/--parent-provenance`，稳定退出 usage 错误。 | 将旧文件改为当前 v3 兼容入口，统一调用新的 parent-provenance 多 Wave 自测；增加 fail-fast `selftest_all_controls.sh`。 |
| F5-06 | Prerequisites 树哈希和来源绑定 | 【正确/应采纳】 | 仅证明给定 parent tree 可应用 patch，不能证明 parent 只由允许的前置 WORK 构成。 | 引入 `PARENT_PROVENANCE` schema v2：校验直接前置集、传递闭包、integration_order、每个前置 WORK commit/tree，以及 `root..parent` first-parent 提交序列无夹带。 |
| F5-07 | 移除 `/dev/fd` 依赖 | 【正确/应采纳】 | 进程替换 `mapfile < <(...)` 在没有 `/dev/fd` 的受限沙箱中会失败。 | `validate_work_dev.sh` 改用普通文件和 `while read`；所有跨进程结构化数据通过 JSON/临时文件传递。 |
| F5-08 | 保持组织授权待批准 | 【正确/应采纳】 | 当前仍缺可识别批准人、角色、签名、批准范围和允许 Wave；技术自测不能代替组织授权。 | 保持 `PENDING_ORGANIZATIONAL_APPROVAL / DRAFT / BLOCKED`；代码结论 `REJECTED`、UAT `PENDING_TEST_ENV`、DEC-013 与 Gate C `OPEN`。 |

## 总裁决

F5-01～F5-08 均有事实依据，全部采纳。第六轮修补只关闭文档与控制脚本内部缺陷，不宣称 Redemption 代码已经实现、DEV/UAT 已执行或组织授权已取得。
