# Redemption 项目 AI Agent 指令

## 1. 适用范围

本指令适用于所有在 Redemption 项目中工作的 AI Agent，包括但不限于：

* 代码阅读与代码审查；
* Python 代码生成、修改与重构；
* SQL 分析与存储过程审查；
* 需求文档与技术文档审查；
* 测试用例审查；
* 迁移方案与交付物检查；
* ZIP、manifest 和项目文件核验；
* SQL、文档与 Python 实现之间的业务规则对齐；
* 第三方评审意见复核；
* 代码注释生成与维护。

---

## 2. 指令优先级

发生冲突时，按以下优先级执行：

1. 用户当前明确提出的要求；
2. 本文件 `AGENTS.md`；
3. 当前任务适用的项目 Skill；
4. 用户指定的权威需求、SQL、代码或文档；
5. 其他项目材料；
6. 通用开发或审查经验。

不得使用低优先级材料覆盖高优先级指令。

用户在当前对话中明确指定某份材料为权威基线时，该指定行为本身属于第 1 项“用户当前明确提出的要求”，其效力高于 Skill 默认规则（具体业务事实来源与优先级见第 8 节和第 15.1 节）。

如果不同材料之间存在实质冲突，应明确指出冲突，不得静默选择或自行补造业务规则。

如果用户明确规定某类材料之间的优先关系，例如：

* 某个已批准正式决策高于有效 SQL；
* SQL 高于粗粒度需求文档；
* 某份指定方案高于历史方案；
* 某次明确业务决策高于已有实现；

则必须按用户当前指定的优先关系执行。

正式决策文档（例如 `DEC-*`）属于业务事实来源，而不是天然等同于“用户当前指令”。其业务事实优先级按第 8 节和第 15.1 节执行；只有当用户在当前任务中明确确认、指定或覆盖该决策时，相应用户指令本身才同时取得本节第 1 项优先级。

---

## 3. 通用证据约束

进行代码、SQL、文档、测试或交付物审查时，必须遵守以下规则：

1. 只能依据实际可访问并已成功读取的材料作出结论。
2. 禁止推测项目中存在未被材料证明的类、字段、模型、表、函数、流程或文件。
3. 每个“已确认错误”必须提供：

   * 文件名；
   * 类、函数、字段、SQL 对象或文档章节；
   * 原始证据；
   * 判定错误的理由。
4. 无法从现有材料证明的问题，必须归类为“无法验证”，不得列为已确认缺陷。
5. 第三方评审意见必须独立复核，不得直接继承其结论。
6. 必须区分：

   * 已确认错误；
   * 潜在风险；
   * 建议优化；
   * 无法验证。
7. 当前审查版本已经修复的问题，不得继续计入当前版本缺陷。
8. 优先保证事实正确和证据完整，不以增加问题数量为目标。
9. 不得把静态阅读描述为实际测试通过。
10. 不得声称读取、执行或验证了实际上没有处理的文件、命令、测试、压缩包或 manifest。
11. 无法提供准确行号时，应引用最近的类名、函数名、字段名、存储过程名或文档章节，不得编造行号。
12. Python 当前实现只能证明“当前代码如何实现”，除非有其他权威证据支持，否则不得直接反向定义业务需求。
13. 注释只能解释已经确认的业务逻辑、技术约束和实现意图，不得成为新的业务规则来源。
14. Skill 中的示例、映射或说明不得替代当前仓库中的实际代码、SQL、文档、已批准正式决策和用户明确决策。
15. 文件名包含 `DEC-`、`DEC_` 或类似字样本身不能证明该文件是已批准正式决策；必须从实际内容、状态字段、批准记录或用户明确说明确认其状态。

---

## 4. 项目 Skill 清单

本项目目前包含以下三个主要 Skill。

项目 Skill 根据主要 Agent 的自动发现机制分别存放：

* Claude 项目 Skill 通常位于：

  `.claude/skills/<skill-name>/SKILL.md`

* Codex 项目 Skill 通常位于：

  `.agents/skills/<skill-name>/SKILL.md`

上述目录只表示主要的自动发现位置，不限制 Skill 的实际适用主体。只要本文件明确要求某个 Agent 使用某个 Skill，该 Agent 就应按显式路径读取对应 `SKILL.md`，不得因为 Skill 位于另一种 Agent 的默认发现目录而忽略它。

无论位于哪个 Skill 根目录：

* Skill 目录名必须与 `SKILL.md` frontmatter 中的 `name` 完全一致；
* Skill 入口文件必须命名为 `SKILL.md`；
* 每个 Skill 必须位于独立目录中；
* 不得因为目录存在就声称 Skill 已经加载；
* 使用 Skill 前必须实际成功读取对应 `SKILL.md`。

### 4.1 `redemption-file-filter`

路径：

`.claude/skills/redemption-file-filter/SKILL.md`

用途：

* 项目文件扫描前的排除处理；
* 判断哪些备份、副本、旧版本和废弃文件不应读取；
* 排除文件名包含 `_bak`、`_bakN` 或 `_final` 的文件；
* 排除 Skill 中明确列出的废弃 `sql_uat/` 脚本；
* 默认排除 `GraphService.run_bfs`；
* 处理不确定业务口径时，规定有效 SQL 的查证顺序；
* 防止将废弃 SQL 或 Python 当前实现反向当成最终业务规则。

以下任务开始前，必须优先使用该 Skill：

* Read、Glob、Grep、Explore 或全项目扫描；
* 代码审查；
* SQL 审查；
* 迁移检查；
* 项目文件清单建立；
* ZIP 或交付包内容筛选；
* 业务规则、字段含义或计算口径查证。

### 4.2 `redemption-sql-doc-map`

路径：

`.claude/skills/redemption-sql-doc-map/SKILL.md`

用途：

* 查询某个 `sql_uat/CALC_*.sql` 对应的需求文档；
* 查询某个奖项对应的 SQL 和规则文档；
* 查找 Elite、EAB、PE、SE、TB、Honor、Leadership 等模块的需求材料；
* 判断哪些 SQL 已经转换成需求或技术文档；
* 判断哪些 SQL 尚未编写对应文档；
* SQL、文档和 Python 服务之间的对齐分析；
* 编写、补写或修订需求文档前的材料定位。

以下任务必须使用该 Skill：

* “某个 SQL 对应哪份文档”；
* “某个奖项的需求文档在哪里”；
* “某份文档基于哪个 SQL”；
* “哪些 SQL 还没有文档”；
* “检查 SQL 与需求文档是否一致”；
* “根据 SQL 补写或更新需求文档”。

### 4.3 `redemption-comment-style`

路径：

`.agents/skills/redemption-comment-style/SKILL.md`

适用主体：

* Codex；
* Claude；
* 其他任何实际创建、修改、重构或修复 Redemption Python 代码的 AI Agent。

该 Skill 位于 `.agents/skills/`，便于 Codex 自动发现，但其规则并不只适用于 Codex。其他 Agent 在本文件要求使用该 Skill 的任务中，应按上述路径显式读取并执行。

用途：

* 规范 AI Agent 创建和修改 Redemption Python 代码时的注释风格；
* 注释格式以 `User/UserStatsService.py` 中 `update_elite_performance` 的现有风格为主要参考；
* 对具有独立业务含义的逻辑块使用 `# region ...` / `# endregion`；
* 对大型、多阶段业务流程使用 `Step 1`、`Step 2` 等阶段注释；
* 对奖金计算、资格判断、状态转换、锁、幂等、重试、早停、图遍历、数据落库等非显而易见逻辑说明业务目的和设计原因；
* 避免逐行翻译代码或添加无信息量注释；
* 修改已有逻辑时同步维护相关注释，防止注释与实际代码失配。

以下任务必须使用该 Skill：

* 创建新的 Python 业务代码；
* 修改现有 Python 业务逻辑；
* 根据 SQL、需求文档、正式决策、技术方案或审查修复方案实现 Python 代码；
* 修复会改变控制流、计算逻辑、状态转换、并发行为、幂等行为或持久化行为的代码；
* 重构包含复杂业务逻辑的方法；
* 用户明确要求补充、统一或整理 Python 代码注释。

以下任务通常不需要使用该 Skill：

* 只进行代码阅读而不修改代码；
* 只进行缺陷审查而不产生修改后的代码；
* 单纯修改配置值、拼写、import 顺序或其他无业务含义的机械性变更；
* 单独修改 SQL；
* 单独修改文档；
* 单独执行测试而不产生代码修改。

如果任务在审查后继续要求直接修复 Python 代码，则从进入实际代码修改阶段开始，必须加载并使用该 Skill。

---

## 5. Skill 组合规则

三个 Skill 可以根据任务需要组合使用。

不得无条件加载所有 Skill。

### 5.1 文件扫描、代码审查或业务查证

使用顺序：

1. 先使用 `redemption-file-filter`；
2. 排除不应读取的文件；
3. 再读取有效代码、SQL、文档和正式决策材料；
4. 对不确定的业务规则，按第 8 节的业务事实优先级查证。

如果任务只要求审查代码、不要求修改代码，通常不需要加载 `redemption-comment-style`。

### 5.2 SQL 与文档映射

使用顺序：

1. 先使用 `redemption-file-filter` 排除旧版、副本和废弃 SQL；
2. 再使用 `redemption-sql-doc-map` 查找有效 SQL 与文档的对应关系；
3. 如果映射表与当前仓库实际文件不一致，以当前仓库现状为准；
4. 明确指出需要更新 `redemption-sql-doc-map` 的项目。

### 5.3 SQL、文档、正式决策与 Python 实现对齐

使用顺序：

1. 使用 `redemption-file-filter` 确定有效材料范围；
2. 使用 `redemption-sql-doc-map` 找到对应 SQL 和文档；
3. 检查当前任务是否存在已批准且仍有效的正式决策（例如 `DEC-*`）；
4. 分别读取：

   * 用户当前明确决策或用户指定权威材料；
   * 已批准且仍有效的正式决策；
   * 有效 SQL；
   * 对应需求或技术文档；
   * 当前 Python 实现；
5. 按第 8 节和第 15.1 节确定业务事实优先级；
6. 如果 SQL 无法确认且不存在更高优先级正式决策，应标记为“需要业务方确认”；
7. 不得直接把 Python 当前实现反向定义为业务规则；
8. 如果任务最终要求修改 Python 代码，再加载并使用 `redemption-comment-style`。

### 5.4 Python 代码生成或修改

凡任务需要创建或修改 Python 业务代码时，使用顺序：

1. 如果需要扫描项目、读取多个文件或查证业务规则，先使用 `redemption-file-filter` 确定有效材料范围；
2. 如果涉及奖金、等级、业务规则或 SQL 与文档关系，按需要使用 `redemption-sql-doc-map` 定位对应材料；
3. 检查是否存在对当前事项具有约束力的用户明确决策或已批准且仍有效的正式决策；
4. 根据第 8 节和第 15.1 节确定最终业务事实基线；
5. 在实际编写或修改 Python 代码前，必须成功读取并使用 `redemption-comment-style`；
6. 按 `redemption-comment-style` 同步生成或维护注释；
7. 完成代码修改后，检查代码逻辑与注释是否一致；
8. 不得为了满足注释格式要求而修改无关业务逻辑。

对于包含复杂业务逻辑的方法：

* 具有独立业务职责的代码块原则上使用：

  `# region ...`

  `# endregion`

* region 标题应使用简洁、准确的中文业务含义；

* 大型、多阶段流程使用 `Step 1`、`Step 2`、`Step 3` 等阶段分隔；

* Step 用于描述较大的处理阶段；

* region 用于描述阶段内具有独立职责的逻辑块；

* 不应为了形式而把每几行代码机械拆成 region；

* 参数验证、幂等、锁、重试、状态转换、资格判断、奖金计算、早停、落库前验证等具有独立业务或技术语义的代码，应优先形成明确逻辑段；

* 对公式、阈值、状态变化、早停条件、锁顺序、并发约束等容易被误改的逻辑，应解释其目的或原因；

* 不得仅把代码语句翻译成中文作为注释。

`redemption-comment-style` 只负责：

* 注释结构；
* 可读性；
* 逻辑分段；
* 意图、约束和原因说明。

它不得负责：

* 创造业务规则；
* 推断未确认阈值；
* 决定奖金公式；
* 覆盖用户当前明确决策；
* 覆盖已批准且仍有效的正式决策；
* 在存在更高优先级正式决策时机械套用 SQL；
* 替代有效 SQL；
* 把现有 Python 行为定义为业务需求。

---

## 6. Skill 加载要求

1. 根据当前任务选择相关 Skill，不要无条件加载全部 Skill。
2. 任务同时涉及文件筛选和 SQL 文档映射时，可以同时使用 `redemption-file-filter` 和 `redemption-sql-doc-map`。
3. 不得仅因为 Skill 路径存在，就声称已经读取或调用了该 Skill。
4. 使用 Skill 前必须确认对应的 `SKILL.md` 已成功读取。
5. 如果 Skill 或其依赖文件无法读取：

   * 明确说明缺失或无法读取的路径；
   * 列出因此未加载的规则；
   * 不得猜测 Skill 内容；
   * 只能依据实际读取到的材料继续处理。
6. 用户明确要求检查默认排除的文件时，应遵循用户要求读取该文件，但必须说明这是排除规则的例外。
7. 任何 AI Agent 当前任务涉及 Python 业务代码创建、修改、重构或修复时，必须在实际修改代码前成功读取：

   `.agents/skills/redemption-comment-style/SKILL.md`
8. 不得仅因为生成的代码已经包含部分 `region` 或中文注释，就声称已经使用 `redemption-comment-style`。
9. 如果 `redemption-comment-style` 无法读取：

   * 明确报告无法读取的路径；
   * 不得声称已经应用该 Skill；
   * 可以依据当前目标文件已有风格尽量保持注释一致；
   * 不得因此自行改变业务逻辑。
10. 如果 Skill 引用了附加 reference、script 或其他依赖文件，只有在当前任务需要对应规则时才读取，不得为了形式无条件加载全部依赖。
11. 如果实际仓库中的 Skill 名称、路径或 frontmatter 与本文件不一致，以实际文件为事实依据，并明确指出 `AGENTS.md` 需要同步更新。

---

## 7. 项目文件默认排除原则

具体排除清单以 `redemption-file-filter` 为准。

在未加载该 Skill 前，不得凭记忆声称某个具体文件属于排除清单。

加载该 Skill 后，默认执行：

* 跳过文件名中包含 `_bak`、`_bakN` 或 `_final` 的文件；
* `_final` 指文件名中的英文字面子串；
* `Doc/` 下 `_最终版`、`_定稿` 等中文命名的文件是有效文档，不适用上述 `_final` 英文字面排除规则；
* 跳过 Skill 明确列出的旧版、副本和废弃 SQL；
* 默认不读取或评审 `GraphService.run_bfs`；
* 用户明确要求读取上述对象时除外。

排除文件不得作为以下结论的证据来源：

* 当前生产逻辑；
* 当前业务规则；
* 当前迁移状态；
* 当前版本缺陷；
* SQL 与文档映射。

如果某个排除文件仅用于历史差异比较，应明确标记为历史材料，不得将其内容混入当前版本事实结论。

---

## 8. 业务规则查证原则

业务规则、计算口径、边界条件或字段含义不明确时，不得直接套用单一来源。应按以下业务事实优先级查证：

1. 用户当前明确提出的业务决策，以及用户在当前任务中明确指定的权威基线或覆盖关系；
2. 已批准、已关闭/确认且仍然有效、未被后续决策取代的正式决策记录，例如项目正式采用的 `DEC-*`；
3. 有效 `sql_uat/` 中能够直接确认的业务规则；
4. 经过确认的专项需求、技术方案或细化需求文档；
5. `Doc/奖金制度.md` 等粗粒度业务制度或概括性材料；
6. 当前 Python 实现，仅作为当前实现现状证据，不作为需求反推依据。

执行时必须遵守：

1. 用户当前明确决策始终可以覆盖历史正式决策、SQL 和文档。
2. 已批准且仍有效的正式决策（如 `DEC-*`）如果明确决定了某项业务口径，则该决策高于有效 SQL。
3. 不得仅因为文件名是 `DEC-*` 就认定其具有第 2 级效力；必须确认其已批准/已关闭/已确认，并且没有被后续正式决策取代。
4. `DRAFT`、`PROPOSED`、`OPEN`、`REJECTED`、已废弃、已撤销或已被 supersede 的决策，不得按已批准正式决策使用。
5. 当不存在更高优先级用户决策或正式决策时，优先检查有效 `sql_uat/`。
6. 不得使用已排除 SQL 作为当前业务规则证据。
7. 有效 SQL 能直接确认且不存在更高优先级决策时：

   * 给出结论；
   * 指明 SQL 文件；
   * 指明存储过程、关键语句或相关代码段。
8. 有效 SQL 无法直接确认，且不存在更高优先级正式决策时：

   * 明确标记为“无法从现有权威材料确认”；
   * 列出需要业务方确认的问题；
   * 不得自行补造规则；
   * 不得把 Python 当前实现直接反向定义为业务需求。
9. 如果更高优先级正式决策与有效 SQL 冲突：

   * 以更高优先级正式决策为当前业务事实；
   * 明确列出与 SQL 的差异；
   * 判断 SQL 是否属于尚未同步、历史实现或待迁移对象；
   * 不得静默把 SQL 描述成最终规则。
10. 如果两个或多个同级、均有效的正式决策之间发生冲突，不得自行选择；必须指出冲突并要求业务方确认或依据后续 superseding 决策解决。

对于当前 Redemption 项目，如果用户已经明确：

* `Doc/奖金制度.md` 属于粗粒度需求材料；
* 当《奖金制度.md》与有效 SQL 存在业务规则冲突时，以有效 SQL 为准；

则该规则只适用于**不存在更高优先级用户决策或已批准正式决策**的场景。

因此，在没有新的用户指令或正式决策覆盖时：

1. 有效 SQL 能明确业务规则的，以有效 SQL 为业务事实基准；
2. `Doc/奖金制度.md` 用于理解业务背景和总体制度；
3. 如果《奖金制度.md》与有效 SQL 冲突，应明确指出冲突；
4. 不得因为《奖金制度.md》的表述与 SQL 不同，就直接判定 SQL 错误；
5. 应继续检查当前 Python 实现是否与当前有效业务事实对齐。

如果两个或多个有效 SQL 自身存在实质冲突，并且没有更高优先级正式决策解决该冲突：

1. 不得自行选取其中一个作为最终规则；
2. 必须指出冲突的 SQL 文件和关键逻辑；
3. 明确列出需要业务方确认的问题；
4. 在业务方确认前，不得把其中任一口径描述成唯一确定需求。

---

## 9. SQL 与文档映射使用原则

`redemption-sql-doc-map` 是速查和路由工具，不是不可变的最终事实。

使用映射结果后，仍应按任务需要确认：

* SQL 文件当前是否存在；
* 文档文件当前是否存在；
* 文件是否被重命名；
* 文档是否确实声明基于对应 SQL；
* SQL 写入表、字段和处理范围是否与文档一致；
* 映射是否因为代码或文档更新而过时；
* 是否存在高于 SQL 的已批准正式决策改变了当前业务口径。

如果仓库现状与 Skill 中的映射不一致：

1. 以实际仓库内容为准；
2. 明确指出不一致；
3. 不得继续使用已失效映射；
4. 建议更新：

   `.claude/skills/redemption-sql-doc-map/SKILL.md`

如果某份需求文档与 SQL 的关系无法从文件内容或项目证据中确认，不得仅依据文件名相似度建立确定映射。

---

## 10. Python 代码与注释规范

Python 代码创建或修改除遵守业务正确性外，还必须遵守本节。

### 10.1 注释基准

主要参考：

`User/UserStatsService.py`

中的：

`update_elite_performance`

现有注释结构。

重点参考：

* `# region ...`
* `# endregion`
* `Step 1`、`Step 2` 等大阶段划分；
* 对业务目的、状态变化、并发原因和关键判断的说明方式。

不得机械复制某个函数的具体文字，应学习其结构和粒度。

### 10.2 region 使用原则

以下类型逻辑在足够复杂或具有独立职责时，优先使用 region：

* 参数验证；
* 输入标准化；
* 初始化；
* 数据加载；
* 幂等验证；
* 获取锁；
* 锁续期；
* 数据预处理；
* 业务资格判断；
* 奖金或业绩计算；
* 状态更新；
* 等级评定；
* 图关系处理；
* 上下级传导；
* 去重；
* 批处理；
* 重试；
* 早停；
* 异常恢复；
* 持久化；
* 落库前最终校验；
* 资源释放。

示例：

```python
# region 参数验证
if not order_id:
    raise ValueError("订单增量处理必须传入 order_id 以保证幂等")
# endregion
```

### 10.3 Step 使用原则

当一个方法存在多个大的业务阶段时，可以使用：

```python
# ---------------------------------------------------------
# Step 1: 处理源用户
# ---------------------------------------------------------
```

Step 内可以继续包含多个 region。

Step 不应替代 region。

简单函数不应为了满足格式要求强制加入 Step。

### 10.4 注释内容要求

优先说明：

* 为什么这样做；
* 业务规则是什么；
* 这个状态代表什么；
* 为什么需要该判断；
* 为什么可以早停；
* 为什么必须先后执行；
* 为什么必须持锁；
* 为什么需要幂等；
* 为什么某个数值或状态发生转换；
* 哪些行为容易在重构中被错误“优化”。

避免：

```python
# 给 count 加 1
count += 1
```

更适合的是：

```python
# 当前节点完成后计入已处理数量，用于控制批次续锁频率。
count += 1
```

仅当该信息确实具有维护价值时才需要添加。

### 10.5 注释事实约束

任何业务注释必须有实际依据，并遵循第 8 节业务事实优先级。

禁止：

* 根据变量名猜业务规则；
* 根据当前 Python 代码推导需求；
* 因为想让 region 标题更完整而补造业务含义；
* 在未确认阈值时写死阈值含义；
* 把推测写成确定事实；
* 在存在更高优先级用户决策或已批准正式决策时，仅依据 SQL 写出相反的业务注释。

例如，在没有证据确认“三条 Elite 线即可晋级 Super Elite”时，不得生成：

```python
# region 满足三条 Elite 线后晋级 Super Elite
```

必须先按第 8 节完成业务规则查证。

### 10.6 修改同步原则

如果代码逻辑改变：

* 同步修改对应注释；
* 删除已经失效的注释；
* 不得保留描述旧逻辑的 region；
* 不得留下“代码已变、注释未变”的状态。

如果只是代码结构变化、业务含义不变，可以调整注释位置，但不得改变其业务含义。

---

## 11. 正式审查流程

正式审查应按以下步骤执行：

1. 明确当前待审目标；
2. 建立本轮可访问材料清单；
3. 使用 `redemption-file-filter` 确定有效文件范围；
4. 确认成功读取和未能读取的文件；
5. 确定权威需求或事实基线，并检查是否存在已批准且仍有效的正式决策；
6. 需要时使用 `redemption-sql-doc-map` 找到对应 SQL 和文档；
7. 分别读取并独立核对正式决策、代码、SQL、文档和第三方意见；
8. 按第 8 节确定最终业务事实优先级；
9. 执行当前环境允许且与任务相关的检查；
10. 区分静态检查结果与实际执行结果；
11. 如果任务要求直接修复 Python 代码，在进入修改阶段前加载 `redemption-comment-style`；
12. 修改后检查业务逻辑、测试和注释的一致性；
13. 按证据输出结论。

如果任务只要求审查，不得擅自进入代码修改阶段。

如果任务要求“审查并修复”，应先完成事实核验，再根据已确认问题执行最小范围修改。

---

## 12. 正式审查输出结构

正式审查应根据任务使用以下结构。

### 总结论

使用以下结论之一：

* `ACCEPTED`
* `ACCEPTED WITH CONDITIONS`
* `REJECTED`
* `UNVERIFIABLE`

### 已确认错误

每项至少包含：

* 严重程度；
* 文件及定位；
* 当前内容或行为；
* 原始证据；
* 错误理由；
* 修正建议。

### 潜在风险

只列出已有材料支持、但尚不能确认是实际缺陷的风险。

### 建议优化

列出不影响正确性的非阻断改进。

不得把建议优化包装成已确认错误。

### 无法验证

列出由于以下原因无法确认的事项：

* 文件缺失；
* 文件无法读取；
* 依赖缺失；
* 环境限制；
* 数据缺失；
* 外部服务不可用；
* 测试未执行；
* 运行条件不具备；
* 正式决策状态无法确认。

### 已确认修复

列出历史版本曾存在、但在当前版本已经确认修复的重要问题。

不得继续把已确认修复的问题计入当前缺陷数量。

### 验证边界

明确列出：

* 已读取文件；
* 未读取文件；
* 默认排除文件；
* 用户指定例外读取文件；
* 已读取并确认状态的正式决策；
* 无法确认状态的决策材料；
* 已执行命令或测试；
* 未执行命令或测试；
* 使用的 Skill；
* 未能加载的 Skill；
* 使用的业务基线；
* 仍然存在的限制。

---

## 13. 禁止伪造

禁止伪造或无证据声称以下内容：

* 文件名；
* 文件路径；
* 行号；
* 类名；
* 函数名；
* 字段名；
* 数据库表；
* 存储过程；
* SQL 与文档对应关系；
* 正式决策的存在、状态、批准情况或优先级；
* 测试结果；
* 命令输出；
* ZIP 内容；
* manifest 结果；
* 运行时行为；
* Skill 已加载或已执行；
* Git 分支、commit 或变更内容；
* 某项业务规则已经确认；
* 某个问题已经修复；
* 某个测试已经通过。

禁止把：

* “代码看起来正确”

描述为：

* “测试已经通过”。

禁止把：

* “成功读取文件”

扩大描述为：

* “已经理解并验证整个项目”。

实际处理范围必须与输出声明一致。

---

## 14. 最小修改原则

修改代码、SQL、文档或 Skill 时：

* 保留无关行为；
* 避免无依据的重构；
* 只修改已确认问题；
* 对业务规则变化进行明确说明；
* 需要时补充测试；
* 不得静默改变业务口径；
* 不得顺手修复未被当前任务要求处理的问题，除非该问题会直接阻止当前修改正确完成；
* Skill 内容变化后，应同步检查 `AGENTS.md` 中的路径和路由说明是否仍然有效；
* `AGENTS.md` 中 Skill 数量、名称或路径变化后，应同步核对当前仓库实际结构；
* 创建或修改 Python 业务代码时，应同时遵循 `redemption-comment-style`；
* 新增复杂业务逻辑不得形成大段无结构、无逻辑边界的代码；
* 新增或修改的 region 注释必须与实际代码职责一致；
* 不得为了满足注释规范而进行无关重构；
* 注释不得成为独立于正式决策、SQL、需求、用户决策和代码之外的新业务规则来源；
* 如果仅需补充注释，不得借机改变现有业务行为；
* 如果仅需修复业务逻辑，不得对无关模块做大规模格式化、重命名或重构。

---

## 15. 业务事实来源与注释职责边界

为避免正式决策、SQL、文档、Python 和注释之间形成多个相互冲突的“事实来源”，统一规定：

### 15.1 业务事实来源

业务事实必须按以下优先级确定：

1. 用户当前明确业务决策，以及用户当前明确指定的权威基线或覆盖关系；
2. 已批准、已关闭/确认、仍然有效且未被后续决策取代的正式决策，例如适用于当前事项的 `DEC-*`；
3. 有效 `sql_uat/` 中可以直接确认的业务规则；
4. 已确认的专项需求、细化需求或技术方案；
5. `Doc/奖金制度.md` 等粗粒度或概括性需求材料；
6. 当前 Python 实现，仅作为实现现状证据。

补充规则：

* 用户当前明确决策高于任何历史 `DEC-*`、SQL 和文档。
* 已批准且仍有效的正式决策高于有效 SQL。
* 有效 SQL 高于与其冲突的粗粒度《奖金制度.md》，但前提是不存在更高优先级用户决策或已批准正式决策。
* `DEC-*` 只有在实际材料证明其已批准/已关闭/已确认并仍有效时，才能获得第 2 级业务事实优先级。
* 草案、待确认、被否决、已废弃、已撤销或已被后续决策取代的 `DEC-*` 不得作为第 2 级权威来源。
* 如果同级权威材料之间冲突，应明确暴露冲突，不得自行补造优先关系。

### 15.2 Skill 的职责

`redemption-file-filter`：

负责决定“哪些材料属于当前有效审查范围”。

`redemption-sql-doc-map`：

负责决定“应该去哪里找到对应 SQL 和文档”。

`redemption-comment-style`：

负责决定“已经确认的逻辑应该如何在代码中被清晰注释和分段”。

三个 Skill 均不得自行创造新的业务规则，也不得覆盖第 15.1 节确定的业务事实优先级。

### 15.3 Python 注释职责

Python 注释用于：

* 帮助维护人员理解意图；
* 解释不明显的业务判断；
* 解释并发、锁、幂等和状态管理原因；
* 表明大型函数的逻辑阶段；
* 降低未来误改风险。

Python 注释不用于：

* 替代正式决策；
* 替代 SQL；
* 替代需求文档；
* 定义尚未确认的业务规则；
* 隐藏代码与权威业务事实之间的差异；
* 为错误实现提供合理化解释。

如果注释、代码、SQL、正式决策之间不一致，应首先按第 15.1 节查证业务事实，再同步修正代码与注释。

---

## 16. 最终执行原则

所有 AI Agent 在 Redemption 项目中工作时，应始终遵守：

1. **先确定材料范围，再做判断。**
2. **先确认业务事实及其优先级，再写代码。**
3. **先独立复核证据，再接受第三方结论。**
4. **用户当前明确决策高于历史正式决策、SQL 和文档。**
5. **已批准且仍有效的正式决策高于有效 SQL。**
6. **不存在更高优先级决策时，SQL 能确认的业务规则不从 Python 反推。**
7. **有效 SQL 与粗粒度《奖金制度.md》冲突时，在不存在更高优先级决策的前提下以有效 SQL 为准。**
8. **无法确认的问题明确留给业务方，不自行补造。**
9. **代码修改坚持最小范围原则。**
10. **复杂 Python 业务代码按照项目统一注释规范进行逻辑分段。**
11. **注释解释事实，不创造事实。**
12. **静态检查与实际执行必须严格区分。**
13. **只声明实际完成和实际验证过的工作。**