# Redemption 项目 AI Agent 指令

## 1. 适用范围

本指令适用于所有在 Redemption 项目中工作的 AI Agent，包括但不限于：

* 代码阅读与代码审查；
* SQL 分析与存储过程审查；
* 需求文档与技术文档审查；
* 测试用例审查；
* 迁移方案与交付物检查；
* ZIP、manifest 和项目文件核验；
* SQL、文档与 Python 实现之间的业务规则对齐；
* 第三方评审意见复核。

## 2. 指令优先级

发生冲突时，按以下优先级执行：

1. 用户当前明确提出的要求；
2. 本文件 `AGENTS.md`；
3. 当前任务适用的项目 Skill；
4. 用户指定的权威需求、SQL、代码或文档；
5. 其他项目材料；
6. 通用开发或审查经验。

不得使用低优先级材料覆盖高优先级指令。

用户在当前对话中明确指定某份材料为权威基线时，该指定行为本身属于第 1 项"用户当前明确提出的要求"，其效力高于 Skill 默认规则（衔接第 8 节末段）。

如果不同材料之间存在实质冲突，应明确指出冲突，不得静默选择或自行补造业务规则。

## 3. 通用证据约束

进行代码、SQL、文档、测试或交付物审查时，必须遵守以下规则：

1. 只能依据实际可访问并已成功读取的材料作出结论。
2. 禁止推测项目中存在未被材料证明的类、字段、模型、表、函数、流程或文件。
3. 每个"已确认错误"必须提供：

   * 文件名；
   * 类、函数、字段、SQL 对象或文档章节；
   * 原始证据；
   * 判定错误的理由。
4. 无法从现有材料证明的问题，必须归类为"无法验证"，不得列为已确认缺陷。
5. 第三方评审意见必须独立复核，不得直接继承其结论。
6. 必须区分：

   * 已确认错误；
   * 潜在风险；
   * 建议优化；
   * 无法验证。
7. 当前审查版本已经修复的问题，不得继续计入当前版本缺陷。
8. 优先保证事实正确和证据完整，不以增加问题数量为目标。
9. 不得把静态阅读描述为实际测试通过。
10. 不得声称读取、执行或验证了实际上没有处理的文件、命令、测试、压缩包或 manifest。
11. 无法提供准确行号时，应引用最近的类名、函数名、字段名、存储过程名或文档章节，不得编造行号。

## 4. 项目 Skill 清单

本项目目前包含以下两个 Skill。

Skill 的目录名必须与 `SKILL.md` frontmatter 中的 `name` 完全一致，且文件必须命名为 `SKILL.md`，两个 Skill 必须分别放在各自目录下，否则不会被识别和加载。

### 4.1 `redemption-file-filter`

路径：

`.claude/skills/redemption-file-filter/SKILL.md`

用途：

* 项目文件扫描前的排除处理；
* 判断哪些备份、副本、旧版本和废弃文件不应读取；
* 排除文件名包含 `_bak`、`_bakN` 或 `_final` 的文件；
* 排除 Skill 中明确列出的废弃 `sql_uat/` 脚本；
* 默认排除 `GraphService.run_bfs`；
* 处理不确定业务口径时，规定有效 SQL 的查证顺序；
* 防止将废弃 SQL 或 Python 当前实现反向当成最终业务规则。

以下任务开始前，必须优先使用该 Skill：

* Read、Glob、Grep、Explore 或全项目扫描；
* 代码审查；
* SQL 审查；
* 迁移检查；
* 项目文件清单建立；
* ZIP 或交付包内容筛选；
* 业务规则、字段含义或计算口径查证。

### 4.2 `redemption-sql-doc-map`

路径：

`.claude/skills/redemption-sql-doc-map/SKILL.md`

用途：

* 查询某个 `sql_uat/CALC_*.sql` 对应的需求文档；
* 查询某个奖项对应的 SQL 和规则文档；
* 查找 Elite、EAB、PE、SE、TB、Honor、Leadership 等模块的需求材料；
* 判断哪些 SQL 已经转换成需求或技术文档；
* 判断哪些 SQL 尚未编写对应文档；
* SQL、文档和 Python 服务之间的对齐分析；
* 编写、补写或修订需求文档前的材料定位。

以下任务必须使用该 Skill：

* "某个 SQL 对应哪份文档"；
* "某个奖项的需求文档在哪里"；
* "某份文档基于哪个 SQL"；
* "哪些 SQL 还没有文档"；
* "检查 SQL 与需求文档是否一致"；
* "根据 SQL 补写或更新需求文档"。

## 5. Skill 组合规则

两个 Skill 可以组合使用。

### 5.1 文件扫描、代码审查或业务查证

使用顺序：

1. 先使用 `redemption-file-filter`；
2. 排除不应读取的文件；
3. 再读取有效代码、SQL 和文档；
4. 对不确定的业务规则，优先从有效 `sql_uat/` 查证。

### 5.2 SQL 与文档映射

使用顺序：

1. 先使用 `redemption-file-filter` 排除旧版、副本和废弃 SQL；
2. 再使用 `redemption-sql-doc-map` 查找有效 SQL 与文档的对应关系；
3. 如果映射表与当前仓库实际文件不一致，以当前仓库现状为准；
4. 明确指出需要更新 `redemption-sql-doc-map` 的项目。

### 5.3 SQL、文档与 Python 实现对齐

使用顺序：

1. 使用 `redemption-file-filter` 确定有效材料范围；
2. 使用 `redemption-sql-doc-map` 找到对应 SQL 和文档；
3. 分别读取：

   * 有效 SQL；
   * 对应需求或技术文档；
   * 当前 Python 实现；
4. 以用户指定的权威基线为准进行对齐；
5. 如果用户没有指定权威基线：

   * 已确认业务规则优先依据有效 `sql_uat/`；
   * `Doc/` 和 Python 代码用于理解上下文、定位实现和发现差异；
   * SQL 无法确认的问题应标记为"需要业务方确认"。

## 6. Skill 加载要求

1. 根据当前任务选择相关 Skill，不要无条件加载全部 Skill。
2. 任务同时涉及文件筛选和 SQL 文档映射时，可以同时使用两个 Skill。
3. 不得仅因为 Skill 路径存在，就声称已经读取或调用了该 Skill。
4. 使用 Skill 前应确认对应的 `SKILL.md` 已成功读取。
5. 如果 Skill 或其依赖文件无法读取：

   * 明确说明缺失路径；
   * 列出未加载的规则；
   * 不得猜测 Skill 内容；
   * 只能依据实际读取到的材料继续处理。
6. 用户明确要求检查默认排除的文件时，应遵循用户要求读取该文件，但必须说明这是排除规则的例外。

## 7. 项目文件默认排除原则

具体排除清单以 `redemption-file-filter` 为准。

在未加载该 Skill 前，不得凭记忆声称某个具体文件属于排除清单。

加载该 Skill 后，默认执行：

* 跳过文件名中包含 `_bak`、`_bakN` 或 `_final` 的文件（`_final` 指文件名中的英文字面子串；`Doc/` 下 `_最终版`、`_定稿` 等中文命名的文件是有效文档，不适用本排除规则）；
* 跳过 Skill 明确列出的旧版、副本和废弃 SQL；
* 默认不读取或评审 `GraphService.run_bfs`；
* 用户明确要求读取上述对象时除外。

排除文件不得作为以下结论的证据来源：

* 当前生产逻辑；
* 当前业务规则；
* 当前迁移状态；
* 当前版本缺陷；
* SQL 与文档映射。

## 8. 业务规则查证原则

当业务规则、计算口径、边界条件或字段含义不明确时：

1. 先检查有效 `sql_uat/`；
2. 不得使用已排除 SQL 作为证据；
3. 有效 SQL 能直接确认时：

   * 给出结论；
   * 指明 SQL 文件；
   * 指明存储过程、关键语句或相关代码段；
4. 有效 SQL 无法直接确认时：

   * 明确标记为"无法从 SQL 确认"；
   * 列出需要业务方确认的问题；
   * 不得自行补造规则；
   * 不得把 Python 当前实现直接反向定义为业务需求。

如果用户明确指定某份业务制度、需求文档或其他材料为最高权威基线，应优先遵循用户指定的基线，并说明其与 SQL 的差异。

## 9. SQL 与文档映射使用原则

`redemption-sql-doc-map` 是速查和路由工具，不是不可变的最终事实。

使用映射结果后，仍应按任务需要确认：

* SQL 文件当前是否存在；
* 文档文件当前是否存在；
* 文件是否被重命名；
* 文档是否确实声明基于对应 SQL；
* SQL 写入表、字段和处理范围是否与文档一致；
* 映射是否因为代码或文档更新而过时。

如果仓库现状与 Skill 中的映射不一致：

1. 以实际仓库内容为准；
2. 明确指出不一致；
3. 不得继续使用已失效映射；
4. 建议更新：
   `.claude/skills/redemption-sql-doc-map/SKILL.md`。

## 10. 正式审查流程

正式审查应按以下步骤执行：

1. 明确当前待审目标；
2. 建立本轮可访问材料清单；
3. 使用 `redemption-file-filter` 确定有效文件范围；
4. 确认成功读取和未能读取的文件；
5. 确定权威需求或事实基线；
6. 需要时使用 `redemption-sql-doc-map` 找到对应 SQL 和文档；
7. 独立核对代码、SQL、文档和第三方意见；
8. 执行当前环境允许且与任务相关的检查；
9. 区分静态检查结果与实际执行结果；
10. 按证据输出结论。

## 11. 正式审查输出结构

正式审查应根据任务使用以下结构：

### 总结论

使用以下结论之一：

* `ACCEPTED`
* `ACCEPTED WITH CONDITIONS`
* `REJECTED`
* `UNVERIFIABLE`

### 已确认错误

每项至少包含：

* 严重程度；
* 文件及定位；
* 当前内容或行为；
* 原始证据；
* 错误理由；
* 修正建议。

### 潜在风险

只列出已有材料支持、但尚不能确认是实际缺陷的风险。

### 建议优化

列出不影响正确性的非阻断改进。

### 无法验证

列出由于文件缺失、依赖缺失、环境限制、数据缺失或未能执行而无法确认的事项。

### 已确认修复

列出历史版本曾存在、但在当前版本已经确认修复的重要问题。

### 验证边界

明确列出：

* 已读取文件；
* 未读取文件；
* 已执行命令或测试；
* 未执行命令或测试；
* 使用的 Skill；
* 未能加载的 Skill；
* 仍然存在的限制。

## 12. 禁止伪造

禁止伪造或无证据声称以下内容：

* 文件名；
* 文件路径；
* 行号；
* 类名；
* 函数名；
* 字段名；
* 数据库表；
* 存储过程；
* SQL 与文档对应关系；
* 测试结果；
* 命令输出；
* ZIP 内容；
* manifest 结果；
* 运行时行为；
* Skill 已加载或已执行。

## 13. 最小修改原则

修改代码、SQL、文档或 Skill 时：

* 保留无关行为；
* 避免无依据的重构；
* 只修改已确认问题；
* 对业务规则变化进行明确说明；
* 需要时补充测试；
* 不得静默改变业务口径；
* Skill 内容变化后，应同步检查 `AGENTS.md` 中的路径和路由说明是否仍然有效。